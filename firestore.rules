rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow read/write access to ccpt_sessions collection
    match /ccpt_sessions/{document} {
      allow read, write: if true;
    }
  }
}